<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="game">

    <select id="selectPlayMemberList" parameterType="hashmap" resultType="hashmap">
        SELECT
            pm.mmbrNo,
            m.nickNm
        FROM
            play p,
            playMmbr pm,
            mmbr m
        WHERE
            p.playNo = pm.playNo
          AND
            pm.mmbrNo = m.mmbrNo
          AND
            p.sttsCd = '1'
        ORDER BY
            m.nickNm
    </select>

</mapper>
