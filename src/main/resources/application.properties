# Server configuration
server.port=8080
server.servlet.context-path=/
logging.charset.console=UTF-8
logging.charset.file=UTF-8

# JSP configuration
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# Static resources
spring.web.resources.static-locations=classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:src/main/webapp/

# MyBatis configuration (if needed to override)
# mybatis.mapper-locations=classpath:mapper/**/*.xml

# Jasypt password
jasypt.encryptor.password=bogopa

# Allow Bean Overriding (for legacy XML settings)
spring.main.allow-bean-definition-overriding=true

# Import legacy properties
spring.config.import=optional:classpath:jdbc.properties


# Spring Boot 표준 DB 설정 (Jasypt가 자동 적용됨)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://203.245.28.115/bogopa?zeroDateTimeBehavior=convertToNull&useUnicode=yes&characterEncoding=UTF-8&autoReconnection=true
spring.datasource.username=system
spring.datasource.password=${jdbc.password}